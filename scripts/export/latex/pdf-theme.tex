% =============================================================================
% ProfBook v4 — Professional Book Theme for Pandoc/XeLaTeX
% =============================================================================
%
% Designed for:  AI-Assisted Professional Engineering with .NET
% Engine:        XeLaTeX (--pdf-engine=xelatex)
% Inclusion:     --include-in-header=pdf-theme.tex
%
% PREREQUISITES:
%   Install a TeX distribution (MiKTeX or TeX Live) with:
%     xelatex, libertinus, microtype, titlesec, titletoc, fancyhdr,
%     booktabs, tabularx, array, caption, enumitem, tcolorbox, framed,
%     fancyvrb, fvextra, etoolbox, textcomp, eurosym, mathspec, hyperref,
%     xcolor, geometry, setspace, ragged2e, float
%
%   MiKTeX (Windows): https://miktex.org/download
%   TeX Live:         https://tug.org/texlive/
%
%   After installation, install Libertinus via:
%     tlmgr install libertinus libertinus-fonts
%   (or use MiKTeX Package Manager)
% =============================================================================

% ---- 1. ENGINE CHECK ----
\RequirePackage{ifxetex}
\RequirePackage{ifluatex}
\ifxetex\else\ifluatex\else
  \PackageError{ProfBook}{XeLaTeX or LuaLaTeX required}{This theme requires
  XeLaTeX or LuaLaTeX. Compile with: xelatex -shell-escape filename.tex}
\fi\fi

% ---- 3. FONTS ----
\usepackage{libertinus}
% \setmainfont{EB Garamond}[Ligatures=TeX, Numbers=OldStyle]
% \setsansfont{Fira Sans}[Scale=MatchLowercase]
% \setmonofont{JetBrains Mono}[Scale=MatchLowercase, Ligatures=NoRequired]

% ---- 3. TYPOGRAPHIC ENHANCEMENTS ----
\microtypesetup{
                protrusion=true,
                tracking=true,
                factor=1100}
\usepackage{ragged2e}
\usepackage{setspace}
\setstretch{1.05}

% ---- 4. PAGE GEOMETRY (A4 / 6x9) ----
\usepackage{geometry}
\geometry{
  a4paper,
  top=2.0cm,
  bottom=2.5cm,
  inner=2.8cm,
  outer=1.8cm,
  includehead,
  includefoot,
  headheight=14pt,
  headsep=0.3cm,
  footskip=0.8cm,
  bindingoffset=0.5cm,
}

% ---- 5. BACKSLASH ESCAPE COMMANDS (for code blocks) ----
\def\n{\textbackslash{}n}

\makeatletter
% ---- 6. ABSTRACT ENVIRONMENT (for document classes that don't define it) ----
\@ifundefined{abstract}{%
  \newenvironment{abstract}
    {\section*{\abstractname}\small\itshape}
    {\par\vspace{6pt}}
}{}

% ---- 6. FRONT/BACK MATTER (only define if not already defined by document class) ----
\@ifundefined{frontmatter}{%
  \newcommand{\frontmatter}{%
    \pagenumbering{roman}%
    \pagestyle{plain}%
  }
}{}
\@ifundefined{mainmatter}{%
  \newcommand{\mainmatter}{%
    \pagenumbering{arabic}%
    \pagestyle{fancy}%
  }
}{}
\@ifundefined{backmatter}{%
  \newcommand{\backmatter}{%
    \pagestyle{plain}%
  }
}{}
\makeatother

% ---- 7. SHADED ENVIRONMENT (guard against undefined) ----
\makeatletter
\@ifundefined{Shaded}{%
  \newenvironment{Shaded}{\snugshade\small}{\endsnugshade}
}{}
\makeatother

% ---- 8. HEADERS & FOOTERS ----
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}

\fancyhead[LE]{\small\sffamily\nouppercase{\leftmark}}
\fancyhead[RO]{\small\sffamily\nouppercase{\rightmark}}
\fancyfoot[LE,RO]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[LE,RO]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

\fancypagestyle{titlepage}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

% ---- 9. CHAPTER & SECTION STYLING ----
\usepackage{titlesec}
\usepackage{titletoc}

\titleformat{\chapter}[display]
  {\sffamily\bfseries}
  {\vspace{-20pt}\textcolor{teal!70!black}{\thechapter}}
  {18pt}
  {\Huge}
  [{\vspace{8pt}{\color{teal!30!white}\rule{\textwidth}{0.8pt}}}]

\titlespacing*{\chapter}{0pt}{20pt}{35pt}

\titleformat{\section}
  {\sffamily\bfseries\LARGE}
  {\thesection}
  {1em}
  {}

\titleformat{\subsection}
  {\sffamily\bfseries\Large}
  {\thesubsection}
  {1em}
  {}

\titleformat{\subsubsection}
  {\sffamily\bfseries\normalsize}
  {\thesubsubsection}
  {1em}
  {}

% ---- 10. TABLE OF CONTENTS ----
\contentsmargin{1.5em}
\dottedcontents{chapter}[0pt]{\sffamily\bfseries\large}{2.5em}{1em}
\dottedcontents{section}[1.8em]{\sffamily\normalsize}{2.5em}{1em}
\dottedcontents{subsection}[4em]{\sffamily\small}{2.8em}{1em}

% ---- 11. CODE BLOCKS ----
\usepackage{framed}
\usepackage{tcolorbox}
\usepackage{fvextra}
\usepackage{etoolbox}

\tcbuselibrary{skins, breakable}

\definecolor{codebg}{RGB}{245,247,250}
\definecolor{codeborder}{RGB}{218,224,233}
\definecolor{shadecolor}{RGB}{245,247,250}
\definecolor{codecomment}{RGB}{0,128,0}
\definecolor{codekeyword}{RGB}{0,0,255}
\definecolor{codestring}{RGB}{163,21,21}
\definecolor{codenumber}{RGB}{0,0,0}

% Pandoc emits highlighted code as Shaded + Highlighting when listings is not
% requested. fvextra extends fancyvrb with reliable line breaking.
\renewenvironment{Shaded}
  {\snugshade\small}
  {\endsnugshade}

\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  commandchars=\\\{\},
  fontsize=\small,
  breaklines=true,
  breakanywhere=true,
  breakautoindent=true,
  breaksymbolleft={},
  breaksymbolright={},
  tabsize=4
}

\RecustomVerbatimEnvironment{verbatim}{Verbatim}{
  fontsize=\small,
  breaklines=true,
  breakanywhere=true,
  breakautoindent=true,
  breaksymbolleft={},
  breaksymbolright={},
  tabsize=4
}

% Tango marks comments and diagnostics as italic. On Windows, Courier New's
% italic face does not provide Arabic glyphs, so Arabic comments render as
% missing-character boxes. Keep the color/bold semantics and avoid italic.
\makeatletter
\@ifundefined{CommentTok}{}{%
  \renewcommand{\CommentTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{#1}}%
}
\@ifundefined{CommentVarTok}{}{%
  \renewcommand{\CommentVarTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{#1}}}%
}
\@ifundefined{AnnotationTok}{}{%
  \renewcommand{\AnnotationTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{#1}}}%
}
\@ifundefined{DocumentationTok}{}{%
  \renewcommand{\DocumentationTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{#1}}}%
}
\@ifundefined{InformationTok}{}{%
  \renewcommand{\InformationTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{#1}}}%
}
\@ifundefined{PreprocessorTok}{}{%
  \renewcommand{\PreprocessorTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{#1}}%
}
\@ifundefined{WarningTok}{}{%
  \renewcommand{\WarningTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{\textbf{#1}}}%
}
\makeatother

% ---- 12. INLINE CODE ----
\let\oldtexttt\texttt
\renewcommand{\texttt}[1]{%
  \colorbox{codebg}{\oldtexttt{\textcolor{teal!80!black}{#1}}}%
}

% ---- 13. TABLE STYLING ----
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{longtable}
\usepackage{ltablex}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

\setlength{\abovetopsep}{4pt}
\setlength{\belowbottomsep}{4pt}
\renewcommand{\arraystretch}{1.08}

% ---- 14. CAPTIONS ----
\usepackage{caption}
\captionsetup{
  font={small, sf},
  labelfont={bf, sf},
  justification=centerlast,
  margin=24pt,
  skip=6pt,
}

% ---- 15. BLOCKQUOTES ----
\renewenvironment{quote}
  {\list{}{\leftmargin=18pt\rightmargin=18pt
   \topsep=10pt\partopsep=0pt\parsep=4pt}
   \item\relax\small\itshape}
  {\endlist}

% ---- 16. LISTS ----
\usepackage{enumitem}
\setlist{topsep=6pt, partopsep=2pt, itemsep=2pt, parsep=2pt}
\setlist[itemize]{label=\textbullet}
\setlist[enumerate]{label=\arabic*.}

% ---- 17. DEFINITION LISTS ----
\renewenvironment{description}
  {\list{}{\labelwidth=0pt \itemindent=-\leftmargin
   \let\makelabel\descriptionlabel}}
  {\endlist}

% ---- 18. HYPERLINKS ----
\AtEndPreamble{%
  \hypersetup{%
    colorlinks=true,
    linkcolor=teal!70!black,
    urlcolor=blue!60!black,
    citecolor=teal!70!black,
    bookmarks=true,
    bookmarksdepth=3,
    bookmarksnumbered=true,
    pdfauthor={Tarek Najem},
    pdftitle={AI-Assisted Professional Engineering with .NET},
    pdfsubject={Software Engineering / .NET Architecture},
    pdfkeywords={.NET, AI, Software Engineering, Clean Architecture, DDD},
    pdfcreator={Pandoc with ProfBook v4},
  }%
}

% ---- 19. WIDOW/ORPHAN CONTROL ----
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000
\renewcommand{\topfraction}{0.85}
\renewcommand{\bottomfraction}{0.85}
\renewcommand{\textfraction}{0.1}
\renewcommand{\floatpagefraction}{0.75}

% ---- 20. FLOAT PLACEMENT ----
\usepackage{float}
\floatplacement{figure}{H}
\floatplacement{table}{H}

% Scale large images to fit within text area
\setkeys{Gin}{width=\textwidth,height=0.8\textheight,keepaspectratio}

% Center all float content
\makeatletter
\g@addto@macro\@floatboxreset{\centering}
\makeatother

% ---- 21. BACKGROUND SHIPOUT PICTURE (full-page image without geometry change) ----
\usepackage{eso-pic}

% ---- 22. BACK COVER SUPPORT ----
% Usage: \backcover{path/to/back-cover.jpg}
\newcommand{\backcover}[1]{%
  \clearpage
  \thispagestyle{empty}
  \AddToShipoutPicture*{%
    \AtPageLowerLeft{%
      \includegraphics[width=\paperwidth,height=\paperheight]{#1}%
    }%
  }%
  \null
}

% ---- 23. SPECIAL CHARACTERS ----
\usepackage{textcomp}
\usepackage{eurosym}

% ---- 24. PDF METADATA ----
\AtBeginDocument{%
  \hypersetup{%
    pdftitle={AI-Assisted Professional Engineering with .NET},
    pdfauthor={Tarek Najem},
  }%
}

\makeatletter
% ---- 25. TITLE PAGE CUSTOMIZATION ----
% Include front cover image if \coverfront is defined, otherwise show text title page
\@ifundefined{coverfront}{%
  \renewcommand{\maketitle}{%
    \begin{titlepage}
      \thispagestyle{titlepage}
      \null\vfill
      \begin{center}
        {\sffamily\bfseries\Huge\color{teal!70!black} AI-Assisted Professional Engineering with .NET\par}
        \vspace{12pt}
        {\Large\itshape\color{gray!60!black} Mastering Architectural Judgment, Clean Design,\\and Core System Ownership in the Era of Generative AI\par}
        \vspace{24pt}
        {\large Tarek Najem\par}
        \vspace{6pt}
        {\small\color{gray!50!black} Software Architect \textbullet\ .NET Specialist\par}
        \vspace{36pt}
        {\sffamily\small\color{gray!40!black} \today\par}
      \end{center}
      \vfill
    \end{titlepage}
  }
}{%
  \renewcommand{\maketitle}{%
    \thispagestyle{empty}
    \AddToShipoutPicture*{%
      \AtPageLowerLeft{%
        \includegraphics[width=\paperwidth,height=\paperheight]{\coverfront}%
      }%
    }%
    \null
    \newpage
  }
}

% ---- 26. END-DOCUMENT HOOK (back cover) ----
\@ifundefined{coverback}{}{%
  \AtEndDocument{%
    \backcover{\coverback}
  }
}
\makeatother
